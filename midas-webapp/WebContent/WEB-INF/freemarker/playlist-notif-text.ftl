
Your friend ${updateUser.friendlyName} (${updateUser.email}) has updated their playlist '${playlist.title}' (${playlist.streamIds?size} tracks).</p>

To visit the playlist, open the robonobo app, or click here: ${playlistUrl}

<#include "text-footer.ftl">
