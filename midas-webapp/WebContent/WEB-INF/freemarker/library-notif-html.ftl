<#include "html-header.ftl">

<p style="color:#111111">${updateUser.friendlyName} (<a href="mailto:${updateUser.email}" style="color:#5b0d01">${updateUser.email}</a>) has added ${numTrax} tracks to their library.</p>

<p style="color:#111111">You can see these tracks immediately in robonobo. If you have received this mail very recently, you may have to select 'Update friends and playlists' from the Friends menu.</p>
<#include "html-footer.ftl">
